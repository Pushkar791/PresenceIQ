# PresenceIQ

Smart attendance system with face recognition for institutional use.

## Run locally

1. Copy `.env.example` to `.env` in `frontend` and `backend`
2. Start MongoDB: `docker compose up -d`
3. Run `start_all.ps1`
